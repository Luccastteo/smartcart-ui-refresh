import 'package:flutter/material.dart';

class AuthColors {
  static const Color darkBackground = Color(0xFF2C3540);     // Fundo escuro (cinza-azulado)
  static const Color cardBackground = Color(0xFFF5F1E8);     // Bege/creme claro dos cards
  static const Color cardTextPrimary = Color(0xFF1A1A1A);    // Texto preto nos cards
  static const Color cardTextSecondary = Color(0xFF4B5563);  // Texto cinza nos cards
  static const Color darkTextPrimary = Color(0xFFFFFFFF);    // Texto branco no fundo escuro
  static const Color inputLabel = Color(0xFF9CA3AF);         // Cinza claro para labels
  static const Color inputBorder = Color(0xFF4B5563);        // Borda dos inputs
  static const Color accent = Color(0xFFB4D12F);             // Verde limão (cor do PAGLY)
  static const Color welcomeButton = Color(0xFF1F2937);      // Botão escuro da WelcomeScreen
}
