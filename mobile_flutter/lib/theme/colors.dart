import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const background = Color(0xFF262F30);
  static const backgroundSecondary = Color(0xFF2E3839);
  static const surface = Color(0xFF2E3839);
  static const surfaceHighlight = Color(0xFF3A4445);
  static const surfaceCard = Color(0xFF2E3839);
  static const surfaceSection = Color(0xFF1A2021);

  // Borders
  static const border = Color(0xFF3A4445);
  static const borderSubtle = Color(0xFF2E3839);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFA0AEC0);
  static const textOnBrand = Color(0xFF262F30);
  static const muted = Color(0xFF718096);
  static const mutedDark = Color(0xFF4A5556);

  // Accent - Lime
  static const accent = Color(0xFFA3E635);
  static const accentDark = Color(0xFF84CC16);
  static const accentLight = Color(0xFFBEF264);

  // Status
  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  static const info = Color(0xFF3B82F6);

  // Gradient
  static const gradientStart = Color(0xFF8B5CF6);
  static const gradientEnd = Color(0xFF7C3AED);

  // Action
  static const actionPrimary = Color(0xFFFFFFFF);
  static const actionPrimaryActive = Color(0xFFF3F4F6);

  // Coral (for auth screens)
  static const coral = Color(0xFFFB7185);
}
