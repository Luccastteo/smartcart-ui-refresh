name: mobile_flutter
description: "PAGLY - Smart Shopping Cart App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Estado
  flutter_riverpod: ^2.6.1
  
  # Navegação
  go_router: ^13.2.0
  
  # Backend
  supabase_flutter: ^2.3.4
  
  # Câmera e OCR
  camera: ^0.11.3
  google_mlkit_text_recognition: ^0.15.0
  image_picker: ^1.0.7
  
  # Export/Import
  pdf: ^3.10.8
  printing: ^5.12.0
  excel: ^4.0.2
  csv: ^6.0.0
  file_picker: ^10.3.10
  share_plus: ^7.2.2
  path_provider: ^2.1.2
  
  # UI
  qr_flutter: ^4.1.0
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_animate: ^4.5.0
  
  # Utils
  intl: ^0.19.0
  uuid: ^4.3.3
  flutter_dotenv: ^5.1.0
  package_info_plus: ^4.2.0
  url_launcher: ^6.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - .env
